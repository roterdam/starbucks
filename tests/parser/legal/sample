class Program {
  int c[4];

  void main(int e, boolean g) {
    int a,b;
    boolean freaky;

    a = 5 - 6;
    freaky = false;
    c[4] = 0x45AEE;

    if (a == 3) {
      test(e);
      a = 4;
    } else {
      c = (5+6)/(3%2 + 4);
    }
    for (a = 4; a < 10) {
      b = 3;
      callout("method", "1", "2", "3");
    }
    while (c < 200 + 4 / 2 && a == true) {
      main(5, true);
      recurse(c);
    }
  }
  boolean recurse(int n) {
    recurse(n - 1);
  }
}
